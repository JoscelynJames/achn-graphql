{
  "name": "acnh-graphql",
  "version": "1.0.0",
  "description": "Graphql API for Animal Crossing - New Horizons",
  "main": "index.js",
  "scripts": {
    "start": "nodemon app.js",
    "clean": "rm -rf dist && mkdir dist",
    "build": "npm run clean . -d dist --ignore node_modules",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^5.2.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-graphql": "^0.9.0",
    "graphql": "^15.0.0",
    "nodemon": "^2.0.3"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "nodemon": "1.17.5"
  }
}
